const city = {
  cod: 200,
  name: 'Rio de Janeiro',
  main: {
    humidity: 80,
    temp: 21,
  },
  weather: [{
    description: 'overcast clouds',
    main: 'Clear' || 'Clouds' || 'Mist' || 'Rain' || 'Thunderstorm',
  }],
  wind: {
    speed: 4.12,
  },
};

export default city;
